/// <reference types="vite/client" />

// Vite's ambient types, for `import.meta.env`. A reference file rather than a
// `types` entry in tsconfig, because setting `types` there turns off automatic
// @types resolution for everything else.
